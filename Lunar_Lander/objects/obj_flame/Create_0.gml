//each spark has different characteristics to add interest
speed = random_range(0.5,1.5);
direction = random_range(0,360);

//Set how many frames until the spark dies
lifeSpan = 120;

image_blend = choose(c_yellow, c_red, c_orange);